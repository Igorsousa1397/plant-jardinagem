import { EmBreve } from "@/components/ui/EmBreve";
export default function PropostasPage() {
  return <EmBreve titulo="Propostas" descricao="Gerador de proposta com escopo pré-montado — próximo módulo." />;
}
